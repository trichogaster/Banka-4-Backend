name: Deploy
run-name: Deploy for ${{ github.repository }}

on:
  push:
    branches:
      - main

jobs:
  deploy:
    if: github.repository_owner == 'RAF-SI-2025'
    runs-on: ubuntu-latest

    steps:
      - name: Run deploy script
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /home/lonskyne
            ./deploy.sh

            
  # deploy-dev-test:
  #   runs-on: ubuntu-latest
  #   (potentially the workflow from main.yml)